[Service]
# D-Bus activation names /usr/bin/Thunar directly through thunar.service. Route
# it through the same wrapper used by desktop and terminal launches so Garage's
# GTK integration is present regardless of which path creates the singleton.
ExecStart=
ExecStart=%h/.local/bin/thunar --daemon
