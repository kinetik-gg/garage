# push_theme() restarts this unit on every theme apply; a burst of applies
# (testing, garage update, rapid flips) must not trip systemd's default
# rate limit and leave the volume keys dead until someone notices.
[Unit]
StartLimitIntervalSec=30
StartLimitBurst=10
