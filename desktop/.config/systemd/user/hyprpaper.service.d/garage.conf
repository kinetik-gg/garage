[Service]
# Leading `-`: a failed render must degrade the theme, never disable the wallpaper.
ExecStartPre=-%h/.local/bin/garage render-wallpaper
ExecStart=
ExecStart=/usr/bin/hyprpaper --config %h/.local/state/garage/generated/hyprpaper.conf
