[package]
name = "garage-core"
version = "0.1.0"
edition = "2021"

[lib]
name = "garage_core"

[lints]
workspace = true

[dependencies]
regex = "1.12.3"
serde = { version = "1.0.229", features = ["derive"] }
thiserror = "2.0.20"
toml = { version = "1.1.4", features = ["preserve_order"] }
